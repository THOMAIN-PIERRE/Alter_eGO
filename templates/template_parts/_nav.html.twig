  <nav class="navbar navbar-expand-lg navbar-dark

         {% if app.user %}
        {{ app.user.navColor }} 
         {% else %}
        bg-primary
         {% endif %}

        sticky-top" id="navMenuMobile">
			{# <a class="navbar-brand pull-right-top" href="#"><i class="fa fa-cogs" aria-hidden="true"></i> #}
            {# <div class="pull-left-top"><i class="fa fa-cog fa-2x" aria-hidden="true"></i></div> #}
            <a class="navbar-brand" href="{{ path('home', {'id': 1}) }}" id="aide1"><img src="https://i.ibb.co/PsLNgMd/Minist-re-des-solidarit-s-et-de-la-sant-2.png" alt="logo Blog" class="img-fluid" style="width:120px; height=120px;"></a>
            {# <button style="font-size: 2rem; background-color: white;" class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon" style="font-size: 2rem; background-color: black;"></span>
            </button> #}

            <button style="font-size: 2rem" class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon float-right"></span>
    </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">

                    {# <li class="nav-item">
                        <a style="color:white; font-size: 1.8rem" class="nav-link ml-3" href="{{path('main')}}">A Propos</a>
                    </li> #}
                    <li class="nav-item">
                        <a style="color:white; font-size: 1.8rem" class="nav-link ml-3" href="{{path('main')}}">CONSULTER</a>
                    </li>

{# navbar-item li a:hover {
    color: yellow !important;
  } #}


                     <ul class="navbar-nav ml-auto">
                {# {% if app.user %} #}
                        
                    <li class="nav-item dropdown">                                              
                        <a href="#" id="accountDropdownLink" class="nav-link dropdown-toggle ml-3" data-toggle="dropdown" style="color: white; font-size: 1.8rem">
                           PUBLIER
                            {# <img src="{{app.user.avatar}}" style="max-width:55px;" class="rounded-circle"> {{app.user.username}}  #}
                        </a>
                        <div class="dropdown-menu dropdown-menu-right alert alert-primary" aria-labelledby="accountDropdownLink">
                            {# <hr> #}
                            <a href="{{path('main_create')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class=""></i> <b>Article</b></a>
                            {# <hr> #}
                            {# {% if is_granted('ROLE_EDITOR') %} #}
                            <a href="{{path('account_publications')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class="" aria-hidden="true"></i> <b>Vidéo</b></a>
                            {# {% endif %} #}
                            {# <hr> #}
                            <a href="{{path('account_commentaires')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class="" aria-hidden="true"></i> <b>Document .pdf</b></a>
                            {# <hr> #}
                            {# {% if is_granted('ROLE_ADMIN') %} #}
                            <a href="{{path('account_events')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class=""></i> <b>Fiche de lecture</b></a>
                            {# {% endif %} #}
                            {# <hr> #}
                             <a href="{{path('customizer')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class="" aria-hidden="true"></i> <b>Atelier</b></a>
                             {# <hr> #}
                            {# {% if is_granted('ROLE_ADMIN') %} #}
                            <a href="{{path('admin_account_login')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class=""></i> <b>Evènement</b></a>
                            {# {% endif %} #}
                            {# <hr> #}
                            {# {% if is_granted('ROLE_ADMIN') %} #}
                            <a href="{{path('admin_account_login')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class=""></i> <b>Activité</b></a>
                            {# {% endif %} #}
                            {# <hr> #}
                            {# {% if is_granted('ROLE_ADMIN') %} #}
                            <a href="{{path('admin_account_login')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class=""></i> <b>Jeu</b></a>
                            {# {% endif %} #}
                            {# <hr> #}
                            {# {% if is_granted('ROLE_ADMIN') %} #}
                            <a href="{{path('admin_account_login')}}" class="dropdown-item p-3 text-left" style="color: black;"><i class=""></i> <b>Carte défi</b></a>
                            {# {% endif %} #}
                            
                        </div>
                    </li>
                {# {% else %} #}
                    {# <li class="nav-item">
                        <a href="{{path('security_registration')}}" class="nav-link" style="color:white; font-size: 1.3rem">Inscription</a>
                    </li> #}
                    {# <li class="nav-item">
                        <a href="{{path('security_login')}}" class="nav-link" style="color:white; font-size: 1.8rem;"> Connexion</a>
                    </li>
                {% endif %} #}
                </ul>



                    {# {% if is_granted('ROLE_EDITOR') %} #}
                        {# <a style="color:white; font-size: 1.8rem" class="nav-link ml-3" href="{{path('main_create')}}">PUBLIER</a> #}
                    {# {% endif %} #}
                    </li>
					{# <li class="nav-item">
                        <a style="color:white; font-size: 1.8rem" class="nav-link ml-3" href="{{path('contact')}}">NOUS CONTACTER</a>
                    </li> #}

                    {# <li class="nav-item">
                        <a style="color:yellow; font-size: 1.8rem" class="nav-link ml-3" href="">JOUER</a>
                    </li>
                    <li class="nav-item">
                        <a style="color:yellow; font-size: 1.8rem" class="nav-link ml-3" href="">SE REUNIR</a>
                    </li> #}
                    
                    <li class="nav-item">
                        <a style="color:white; font-size: 1.8rem" class="nav-link ml-3" href="{{path('event_index')}}">EVENEMENTS</a>
                    </li>
                    <li class="nav-item">
                        <a style="color:white; font-size: 1.8rem" class="nav-link ml-3" href="{{path('contact')}}">NOUS CONTACTER</a>
                    </li>
                    {# <li class="nav-item">
                        <a class="nav-link text-dark " href="#!" id="step-5">
                            <button type="button" style="border: none; background-color: white;" id="aidePageAccueil"><i class="fas fa-question-circle fa-2x"></i></button>
                        </a>
					</li> #}

                </ul>
                 <ul class="navbar-nav ml-auto">
                {% if app.user %}
                        {# <i class="fas fa-bookmark fa-2x mt-2" style="color: yellow;"></i>
                        <p style="width: 40px; height: 38px; border-radius: 50%; background-color: #C4C4C4; text-align: center; padding: 8px; font-weight: bold;"> 2</p>

                        <i class="fas fa-bell fa-2x mt-2" style="color: yellow;"></i>
                        <p style="width: 40px; height: 38px; border-radius: 50%; background-color: #C4C4C4; text-align: center; padding: 8px; font-weight: bold;"> 2</p> #}
                        
                    <li class="nav-item dropdown">
                        <div class="row text-center ml-auto">
                        <div class="row text-center mr-2 col-md-6">
                        <i class="fas fa-clipboard-check fa-2x mt-2 mr-2" style="color: yellow;"></i>
                        <p style="width: 40px; height: 38px; border-radius: 50%; background-color: #C4C4C4; text-align: center; padding: 8px; font-weight: bold;"> 1</p>
                        </div>
                        <i class="fas fa-bell fa-2x mt-2 mr-2" style="color: yellow;"></i>
                        <p style="width: 40px; height: 38px; border-radius: 50%; background-color: #C4C4C4; text-align: center; padding: 8px; font-weight: bold;"> 2</p>
                        </div>
                        <!--Grid column-->
                        {# <div class="col-md-6 mb-2">
                                <i class="fas fa-bookmark fa-2x mt-2" style="color: yellow;"></i>
                                <p style="width: 40px; height: 38px; border-radius: 50%; background-color: #C4C4C4; text-align: center; padding: 8px; font-weight: bold;"> 2</p>
                        </div> #}
                        <!--Grid column-->

                        <!--Grid column-->
                        {# <div class="col-md-6 mb-2">                      

                            <i class="fas fa-bell fa-2x mt-2" style="color: yellow;"></i>
                            <p style="width: 40px; height: 38px; border-radius: 50%; background-color: #C4C4C4; text-align: center; padding: 8px; font-weight: bold;"> 2</p>

                        </div> #}
          

                        {# </div> #}
                        <!--Grid row-->
                        
                        <a href="#" id="accountDropdownLink" class="nav-link dropdown-toggle" data-toggle="dropdown" style="color: white; font-size: 1.3rem">

                            <img src="{{app.user.avatar}}" style="max-width:55px; border-radius:50%"> {{app.user.username}} 

                            {# <img src="{{app.user.avatar}}" style="max-width:55px;" class="rounded-circle"> {{app.user.username}}  #}
                        </a>
                        <div class="dropdown-menu dropdown-menu-right alert alert-primary" aria-labelledby="accountDropdownLink">
                            <a href="{{path('account_index')}}" class="dropdown-item p-3 text-left"><i class="fas fa-user-circle"></i> <b>Mon compte</b></a>

                            {% if is_granted('ROLE_EDITOR') %}
                            
                            <a href="{{path('account_publications')}}" class="dropdown-item p-3 text-left"><i class="fa fa-th-list" aria-hidden="true"></i> <b>Mes articles</b></a>
                            {% endif %}
                            <a href="{{path('account_commentaires')}}" class="dropdown-item p-3 text-left"><i class="fa fa-comment" aria-hidden="true"></i> <b>Mes commentaires</b></a>
                            {# {% if is_granted('ROLE_ADMIN') %} #}
                            {# <a href="{{path('account_events')}}" class="dropdown-item p-3 text-left"><i class="far fa-calendar-alt"></i> <b>Mes évènements</b></a>
                            {% endif %} #}
                             {# <a href="{{path('customizer')}}" class="dropdown-item p-3 text-left"><i class="fas fa-cog" aria-hidden="true"></i> <b>Mes réglages</b></a> #}
                            {% if is_granted('ROLE_ADMIN') %}
                            {# <a href="{{path('admin_account_login')}}" class="dropdown-item p-3 text-left" style="background-color: #343A40; color: yellow"><i class="fas fa-tools"></i> <b>Administration</b></a> #}
                            <hr>
                            <a href="{{path('admin_account_login')}}" class="dropdown-item p-3 text-left"><i class="fas fa-tools"></i> <b>Administration</b></a>
                            {% endif %}
                            {# <div class="dropdown-divider"></div>  #}
                            {# <a href="{{path('security_logout')}}" class="dropdown-item p-3 text-left" style="background-color: red; color: white"><i class="fas fa-power-off"></i> <b>Déconnexion</b></a> #}
                            <hr>
                            <a href="{{path('security_logout')}}" class="dropdown-item p-3 text-left"><i class="fas fa-power-off"></i> <b>Déconnexion</b></a>
                            
                        </div>
                    </li>
                {% else %}
                    {# <li class="nav-item">
                        <a href="{{path('security_registration')}}" class="nav-link" style="color:white; font-size: 1.3rem">Inscription</a>
                    </li> #}
                    <li class="nav-item">
                        {# <a href="{{path('security_login')}}" class="nav-link" style="color:white; font-size: 1.8rem;"> Connexion</a> #}
                        <a href="{{path('security_login')}}" class="nav-link" style="color: yellow; font-size: 1.8rem;"> Connexion</a>
                    </li>
                {% endif %}
                </ul>
				
                {# <ul class="navbar-nav ml-auto">
                {% if not app.user %}

                    <li class="nav-item">
                        <a href="{{path('security_registration')}}" class="nav-link" style="color:white; font-size: 1.3rem"><i class="fa fa-sign-in" aria-hidden="true"></i> Inscription</a>
                    </li>
                    
                    <li class="nav-item">
                        <a href="{{path('security_login')}}" class="nav-link" style="color:white; font-size: 1.3rem"><i class="fa fa-sign-in" aria-hidden="true"></i> Connexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{path('security_logout')}}" class="nav-link" style="color:white; font-size: 1.3rem"><i class="fa fa-power-off" aria-hidden="true"></i> Deconnexion</a>
                    </li>
                {% endif %}
                </ul> #}
				 
			{# <a class="btn btn-b btn-lg smooth style="position: absolute; right: 0; top: 0;"> <i class="fa fa-cogs" aria-hidden="true"></i></a> #}
			{# <a class="navbar-brand pull-right-top" href="#"><i class="fa fa-cogs" aria-hidden="true"></i> #}
						
					

	
    </div>
    
        <script src="main.js"></script>
        <script src="main2.js"></script>
    </nav>

        {# {% for label, messages in app.flashes %}

        <div class="container">
            <div class="alert alert-{{label}}">
                {% for message in messages %}
                    <p>{{message | raw}}</p>
                {% endfor %}
            </div>
        </div>
            
        {% endfor %} #}
       